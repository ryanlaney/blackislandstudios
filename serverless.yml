# serverless.yml

name: "blackislandstudios"

plugins:
  - serverless-dotenv-plugin

blackislandstudios:
  component: "@sls-next/serverless-component@3.7.0" # it is recommended you pin the latest stable version of serverless-next.js
  inputs:
    domain: ["blackisland.io"]
